= simple_form_for [@store,@manager], remote: true do |f|
  .fields
    .eight.wide.field
      = f.input :name
    .three.wide.field
      = f.input :cpf, input_html: { class: 'cpf'}
    .three.wide.field
      = f.input :telephone
  .field
    = f.button :submit, class: 'ui button blue', data: { disable_with: t(:disable_with)}
